/**
 * assets/js/admin_landing.js
 * Initialisation for the admin landing page.
 */
document.addEventListener('DOMContentLoaded', () => {
    if (typeof lucide !== 'undefined') lucide.createIcons();
});
